{
	"name": "action",
	"version": "0.1.0",
	"description": "changepacks github action",
	"author": "JeongMin Oh",
	"license": "Apache-2.0",
	"scripts": {
		"build": "bun build --target node src/index.ts --production --outfile=dist/index.js --format=cjs",
		"lint": "biome check && tsc --noEmit",
		"lint:fix": "biome check --write && tsc --noEmit",
		"prepare": "husky"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.5",
		"@types/bun": "^1.3",
		"husky": "^9",
		"typescript": "^7.0"
	},
	"dependencies": {
		"@actions/core": "^3.0.1",
		"@actions/exec": "^3.0.0",
		"@actions/github": "^9.1.1",
		"@actions/http-client": "^4.0.1"
	}
}
